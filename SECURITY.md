# أمن لوحة الويب العامة

هذه لوحة اختبار على الجهاز. ليست النواة الكاملة لـ `Waraqa-Code`.

- النص المدخل يُهرّب قبل العرض.
- لا تحليلات ولا CDN.
- `localStorage` تحت `waraqa-v1` غير مشفّر افتراضيًا. عبارة المرور الاختيارية تشفّر الحالة بـ AES-GCM على الجهاز.
- لا عزل بين مستخدمي نفس المتصفح.
- المحرك في المتصفح مكشوف طبعًا. لا تُدّع «حماية خوارزمية».
