# الأمن — صفحة ورقة العامة

الصفحة الحالية ثابتة: لا جافاسكربت، لا تخزين محلي، لا توليد ملفات في المتصفح.

- لا تحليلات ولا CDN.
- لا مفتاح تخزين في هذه الصفحة. نطاق التخزين `waraqa-v1` يخص التطبيق الكامل وحده، وأي نسخة ويب تجريبية مستقبلية تستخدم `waraqa-web-demo-v1` — النطاقان لا يلتقيان أبدًا.
- أبلغ المشرف على المستودع. لا Issue عام باستغلال.

## دين تقني موثّق

- `unsafe-inline` في `script-src` و`style-src` دين تقني موثّق مصدره سلوك TanStack Start في التطبيق الكامل (أنماط ونصوص مضمّنة في الاستجابة)، لا خيار تصميمي في هذه الصفحة. يُسدَّد عندما تُشحن حزم التطبيق خارجية أو بموجزات/أرقام عشوائية لكل مسار، فتُرفع العبارة من السياسة.
